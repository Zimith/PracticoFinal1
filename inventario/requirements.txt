## Dependencias Python del proyecto
## Nota: si actualizás versiones, probá la app localmente y dentro del contenedor Docker.

asgiref==3.9.1
beautifulsoup4==4.13.5
crispy-bootstrap4==2025.6   # utilidades para crispy-forms con Bootstrap 4
Django==5.2.6               # versión principal de Django del proyecto
django-bootstrap4==25.2
django-crispy-forms==2.4    # integración de formularios con Bootstrap
pillow==11.3.0              # soporte para ImageField y procesamiento de imágenes
soupsieve==2.8
sqlparse==0.5.3
typing_extensions==4.15.0
gunicorn                    # servidor WSGI usado en producción dentro del contenedor
psycopg2-binary             # adaptador para Postgres
django-allauth==0.59.0      # autenticación (solo login); registro deshabilitado en settings
whitenoise==6.11.0          # sirve archivos estáticos directamente desde la app (opcional)

# NOTA: se eliminó weasyprint para evitar dependencias nativas complejas en la imagen.
